import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.directick',
  appName: 'directick.com',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
